;;; A package for personalization for Yu Shen

(message "setq user-full-name")
(setq user-full-name "Yu (Aaron) Shen") ; to have the correct author name

;; (setq dotspacemacs-startup-banner nil)    ; to avoid the error message of "PNG warning: Interlace handling should be turned on when using png_read_image"
;; It does not work to prevent the message, and also it does not improve the speed  of start up.

(provide 'yushen-personalization)
